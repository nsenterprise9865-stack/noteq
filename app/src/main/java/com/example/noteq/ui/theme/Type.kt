package com.example.noteq.ui.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.font.FontFamily

// Set of Material typography styles to be used throughout the app.
val Typography = Typography(
    // You can customize the default text styles here if needed.
    // For simplicity we keep the defaults.
)
